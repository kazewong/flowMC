

#DOCS

### Installing the 
conda install sphinx
pip install myst-nb
pip install sphinx-book-theme


#### In folder where Makefile + make.bat are
make html 
# then open in build folder - html.index


### SERVER 

rsync -av --exclude='.git/' --exclude='docs/' --exclude='joss/' NFsampler cholesky:.
srun --time=00:59:00 --account=swim --partition=gpu --pty bash
module load anaconda3 cuda cudnn
conda activate flowmc-gpu

pip install --user gpustat 
gpustat -cpu -i 1


# Pour récupérer des figures sur le cluster for instance
rsync -av cholesky:NFsampler/temp*.png 